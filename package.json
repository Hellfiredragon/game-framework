{
  "devDependencies": {
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^1.1.2",
    "lerna": "^2.11.0",
    "yarn": "^1.7.0"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "O:\\Repositories\\game-framework.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "game-framework:watch": "lerna --scope=game-framework run build:watch --stream",
    "game-framework:test:watch": "lerna --scope=game-framework run test:watch --stream",
    "asteroid-mining:watch": "lerna --scope=asteroid-mining run build:watch --stream",
    "asteroid-mining:test:watch": "lerna --scope=asteroid-mining run test:watch --stream"
  }
}
